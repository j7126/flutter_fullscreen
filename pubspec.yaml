name: flutter_fullscreen
description: "Allow setting and listening to fullscreen state on all platforms."
version: 1.2.0
homepage: https://github.com/j7126/full_screen

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.0
  window_manager: ^0.5.0

dev_dependencies:
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
  web:
